# ViewModels

This directory contains the ViewModels for the application. In an MVVM (Model-View-ViewModel) architecture, the ViewModel acts as an intermediary between the View (UI) and the Model (data and business logic). It prepares data for the View to display and handles UI logic and user input.
