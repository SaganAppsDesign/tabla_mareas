# Utilities

This directory holds shared utility functions, constants, and extensions that can be reused throughout the application. Examples include formatters, validators, or constants for routes and keys.
