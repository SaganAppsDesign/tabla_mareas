# Data Sources

This directory contains classes responsible for fetching data from various sources, such as remote APIs (using `http` or `dio`) or local databases (like `sqflite` or `hive`). These are the most external parts of the data layer.
