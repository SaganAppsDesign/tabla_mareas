# Repositories

This directory contains the abstract definitions (interfaces) of the repositories. These interfaces define the contract for data operations that will be implemented by the data layer, decoupling the domain from the specific data sources.
