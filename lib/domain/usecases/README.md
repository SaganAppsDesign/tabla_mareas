# Use Cases

This directory contains the application-specific business rules. Each use case class represents a single business operation and orchestrates the flow of data between the presentation and data layers.
